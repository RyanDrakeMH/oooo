<Window
    x:Class="WinUI3App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUI3App"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="WinUI 3 - Editor de Texto Nativo de Alto Rendimiento">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header / Toolbar -->
            <RowDefinition Height="*"/>    <!-- Editor Principal -->
            <RowDefinition Height="Auto"/> <!-- Status Bar -->
        </Grid.RowDefinitions>

        <!-- Barra de Herramientas Principal -->
        <Border Grid.Row="0" Background="{ThemeResource LayerFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="0,0,0,1" Padding="12,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Título y Opciones -->
                <StackPanel Orientation="Horizontal" Spacing="16" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                        <FontIcon Glyph="&#xE8A5;" FontSize="20" Foreground="{ThemeResource AccentAAFillColorDefaultBrush}"/>
                        <TextBlock Text="WinUI 3 Text Engine" FontWeight="Bold" FontSize="16" VerticalAlignment="Center"/>
                    </StackPanel>

                    <AppBarSeparator/>

                    <!-- Selector de Fuente -->
                    <ComboBox x:Name="FontFamilyCombo" Header="Tipografía" Width="160" SelectionChanged="OnFontFamilyChanged" SelectedIndex="0">
                        <ComboBoxItem Content="Segoe UI Variable"/>
                        <ComboBoxItem Content="Cascadia Code"/>
                        <ComboBoxItem Content="Consolas"/>
                        <ComboBoxItem Content="Arial"/>
                    </ComboBox>

                    <!-- Tamaño de Fuente -->
                    <NumberBox x:Name="FontSizeBox" Header="Tamaño" Value="14" Minimum="8" Maximum="72" SpinButtonPlacementMode="Inline" Width="100" ValueChanged="OnFontSizeChanged"/>

                    <!-- Ajuste de Línea -->
                    <ToggleButton x:Name="WordWrapToggle" Content="Ajuste de línea" IsChecked="True" Click="OnWordWrapToggled" VerticalAlignment="Bottom" Margin="0,0,0,2"/>

                    <!-- Cambiar a RichEditBox -->
                    <Segmented x:Name="ModeSelector" SelectionChanged="OnModeChanged" SelectedIndex="0" VerticalAlignment="Bottom" Margin="0,0,0,2">
                        <SegmentedItem Content="TextBox Nativo"/>
                        <SegmentedItem Content="RichEditBox Nativo"/>
                    </Segmented>
                </StackPanel>

                <!-- Botones de Acción -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                    <Button Content="Limpiar" Click="OnClearTextClicked"/>
                    <Button Content="Cargar Texto Demostración" Style="{StaticResource AccentButtonStyle}" Click="OnLoadDemoTextClicked"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Área de Edición de Texto Nativa -->
        <Grid Grid.Row="1" Margin="16">
            <!-- Modo 1: TextBox Nativo WinUI 3 (Alta precisión de clic, I-beam caret exacto) -->
            <TextBox x:Name="NativeTextBox"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     ScrollViewer.HorizontalScrollBarVisibility="Auto"
                     ScrollViewer.IsVerticalScrollChainingEnabled="True"
                     FontFamily="Segoe UI Variable"
                     FontSize="14"
                     Padding="12"
                     CornerRadius="8"
                     PlaceholderText="Haz clic en cualquier parte de este texto para verificar cómo el cursor (caret) se coloca de forma milimétrica en el carácter exacto..."
                     SelectionChanged="NativeTextBox_SelectionChanged"
                     TextChanged="NativeTextBox_TextChanged"/>

            <!-- Modo 2: RichEditBox Nativo WinUI 3 (Formato enriquecido + TSF DirectWrite) -->
            <RichEditBox x:Name="NativeRichEditBox"
                         Visibility="Collapsed"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                         ScrollViewer.HorizontalScrollBarVisibility="Auto"
                         FontFamily="Segoe UI Variable"
                         FontSize="14"
                         Padding="12"
                         CornerRadius="8"
                         SelectionChanged="NativeRichEditBox_SelectionChanged"
                         TextChanged="NativeRichEditBox_TextChanged"/>
        </Grid>

        <!-- Barra de Estado Inferior (Status Bar) -->
        <Border Grid.Row="2" Background="{ThemeResource LayerFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="0,1,0,0" Padding="16,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Posición de Cursor y Métricas -->
                <StackPanel Orientation="Horizontal" Spacing="24">
                    <!-- Posición exacta del Caret (Línea:Columna) -->
                    <StackPanel Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
                        <FontIcon Glyph="&#xE762;" FontSize="14" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                        <TextBlock x:Name="CursorPosText" Text="Línea: 1, Columna: 1" FontWeight="SemiBold"/>
                    </StackPanel>

                    <!-- Estadísticas de Caracteres/Palabras -->
                    <StackPanel Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
                        <FontIcon Glyph="&#xE8C8;" FontSize="14" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                        <TextBlock x:Name="StatsText" Text="0 palabras | 0 caracteres"/>
                    </StackPanel>

                    <!-- Estado del Motor de Texto Nativo -->
                    <StackPanel Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
                        <FontIcon Glyph="&#xE9D9;" FontSize="14" Foreground="{ThemeResource SystemControlForegroundAccentBrush}"/>
                        <TextBlock Text="Motor: DirectWrite + Win32 TSF (Clic 100% Preciso)" Foreground="{ThemeResource SystemControlForegroundAccentBrush}" FontSize="12"/>
                    </StackPanel>
                </StackPanel>

                <!-- Estado del Scroll / Rendimiento -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="16">
                    <TextBlock Text="Aceleración por Hardware GPU (Composition API)" FontSize="12" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
